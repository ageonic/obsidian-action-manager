export enum Action {
    Project = "project",
    Task = "task",
    Reminder = "reminder",
    FollowUp = "followup",
    Activity = "activity",
}

export enum Meta {
    Organization = "organization",
    Individual = "individual",
}