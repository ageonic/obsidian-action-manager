export enum Action {
    Task = "task",
    Reminder = "reminder",
    FollowUp = "followup",
    Activity = "activity",
}

export enum Meta {
    Organization = "organization",
    Individual = "individual",
    Project = "project",
}